# DeepSeek API key — stored in the OS keychain at runtime.
# This file is for local reference only. Never commit a real key.
DEEPSEEK_API_KEY=your-deepseek-api-key

# Tavily API key — stored in the OS keychain at runtime.
TAVILY_API_KEY=your-tavily-api-key
